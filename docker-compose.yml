name: sub2api-extra

# Select active services through COMPOSE_PROFILES in compose.services.env:
# docker compose --env-file compose.services.env <command>
include:
  - ./account-manager/compose.yaml
  - ./provider-monitor/compose.yaml
